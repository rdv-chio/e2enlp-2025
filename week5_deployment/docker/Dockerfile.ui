# Use the same stable Python version
FROM python:3.11-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the requirements file from the project root
COPY ./requirements.txt .

# Install all dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the Streamlit UI script into the container
COPY ./week5_deployment/ui/iep_generator_app.py .

# Expose the port Streamlit runs on
EXPOSE 8501

# Run the application
CMD ["streamlit", "run", "iep_generator_app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]